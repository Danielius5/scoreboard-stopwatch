var config = {};


// Set a color that will be used on numbers
config.numberColor = "#f00"; // default - #f00

// Set a background color
config.backgroundColor = "#333"; // default - #333

// Automatically start a clock after resetting
config.startAfterReset = false; // default - false

// (Only useful if startAfterReset is set to true)
// If clock is stopped, reset it without starting
config.onlyResetIfStopped = true; // default - true